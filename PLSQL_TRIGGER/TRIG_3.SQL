SET SERVEROUTPUT ON;
CREATE OR REPLACE TRIGGER EMP_3
AFTER INSERT
ON EMP_02
FOR EACH ROW

DECLARE

MUSER DUAL.USER%TYPE;

BEGIN
IF(MUSER = 'system') THEN
    raise_application_error(-20003,'SYSTEM USER CANNOT PERFORM DML OPERATION');
END IF;
END;
/